#include <iostream>
#include <vector>

typedef long long ll;
using namespace std;

void dfs(int S, int MAX) {
}

int main(void) {

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int M, N;
    cin >> N, M;

    return 0;
}